# Architecture Reference

This repository implements the architecture defined in
`docs/architecture-reference.pdf` (the CI-Agent Production Architecture
Report).

**Status:** the PDF was not present in the repository workspace when Batch 1
was executed, so it could not be committed here. Batch 1 was implemented
strictly from the Batch 1 instructions, which quote the governing requirements
(Sections 1, 2, 4.1, 6, 7, 13 and 14 of the report).

**Action:** place `CI-Agent-Production-Architecture-Report.pdf` in this
directory under the name `architecture-reference.pdf` so section
cross-references in the code, tests and `NOTES.md` can be checked against the
source document.
