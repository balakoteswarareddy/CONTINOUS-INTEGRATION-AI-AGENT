# CI Agent environment template — Batch 1 placeholder.
# Copy to `.env` and populate real values only when later batches introduce
# integrations that need them. Never commit real secrets.

# Runtime environment: local | dev | prod
CI_AGENT_ENV=local
