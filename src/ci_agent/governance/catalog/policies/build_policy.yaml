# Build policy family (CI-Agent Production Architecture Report, Section 6 — Build).
#
# Deny-by-default posture: no base image or egress domain is allowed until
# explicitly approved; builds must finish within the timeout ceiling.
policy_version: "1.0.0"
allowed_base_images: []
allowed_egress_domains: []
max_timeout_seconds: 3600
